; Merge with the SAME inventory files used by your existing GOAD instance.
; Do not duplicate credentials or change GOAD's connection settings here.
[monitoring_windows:children]
domain

[monitoring_controller]
localhost ansible_connection=local ansible_shell_type=sh
